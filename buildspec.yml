version: 0.2

phases:
  pre_build:
    commands:
      - composer install
      
  build:
    commands:
      - vendor/bin/phpunit

  post_build:
    commands:
      - command php artisan migrate --force
      
artifacts:
  files:
    - '**/*'
